## Virtual Environvironment
`python -m venv .venv` or `python3 -m venv .venv`

## Activate Virtual Environment
`source .venv/bin/activate`

## Install Flask
`pip install flask`

## Install ORM [ SQLAlchemy ]
`pip install Flask-SQLAlchemy`